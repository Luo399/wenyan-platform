name: Deploy Frontend to Aliyun OSS

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'public/**'
      - 'package.json'
      - 'vite.config.ts'
      - '.env.production'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build
        env:
          VITE_AUTH_SECRET: ${{ secrets.VITE_AUTH_SECRET }}

      - name: Configure AWS CLI for Aliyun OSS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.OSS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.OSS_ACCESS_KEY_SECRET }}
          aws-region: oss-cn-hangzhou

      - name: Deploy to Aliyun OSS
        run: |
          aws s3 sync ./dist s3://${{ secrets.OSS_BUCKET }} \
            --endpoint-url https://oss-cn-hangzhou.aliyuncs.com \
            --delete \
            --cache-control "public, max-age=31536000"

      - name: Purge CDN cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
        if: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID != '' }}