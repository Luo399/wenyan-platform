你是一个数据转换专家。请根据我提供的 Excel 表格（包含列名：wen_id, question_seq, question_text, type, option_A, option_B, option_C, option_D, option_E, correctAnswer, audio_file, image_file），生成一个 JSON 数组，每个元素代表一道题目。

WEN_id	question_seq	type	question_text	option_A	option_B	option_C	option_D	correctAnswer
WEN_01	WEN_01_Q1	radio	“斯是陋室，惟吾德馨”中的“馨”意思是？	散布很远的香气	温馨	德行	名声	A
WEN_01	WEN_01_Q2	checkbox	下列哪一项不是《陋室铭》中提到的生活场景？	调素琴	阅金经	饮酒赋诗	谈笑有鸿儒	A,C
### 转换规则：
1. **题目编号 id**：用 `${wen_id}_Q${question_seq}` 格式，例如 WEN_01_Q1。
2. **保留原始字段**：`wen_id`, `question_seq`, `question_text` 分别映射为 `wenId`, `questionSeq`, `text`。
3. **type**：直接使用 `radio` 或 `checkbox`。
4. **选项数组 options**：
   - 遍历 `option_A` 到 `option_E`，只保留非空字符串的列。
   - 每个选项对象格式：`{ "id": "A", "label": "选项文字" }`（id 就是字母 A/B/C/D/E）。
5. **正确答案 correctAnswer**：
   - 如果 `type` 为 `radio`，则 `correctAnswer` 为字符串，例如 `"A"`。
   - 如果 `type` 为 `checkbox`，则 `correctAnswer` 为数组，将 `correct` 按逗号分割并去除空格，例如 `["A","C"]`。
6. **音频和图片**：
   - `audioUrl`：直接使用 `audio_file` 的值（字符串）。
   - `imageUrl`：直接使用 `image_file` 的值（字符串）。
   - 如果某个文件名为空，则对应的字段设为 `null`。

### 输出 JSON 格式示例：
[
  {
    "id": "WEN_01_Q1",
    "wenId": "WEN_01",
    "questionSeq": 1,
    "text": "题干文字",
    "type": "radio",
    "options": [
      { "id": "A", "label": "选项A文字" },
      { "id": "B", "label": "选项B文字" },
      { "id": "C", "label": "选项C文字" }
    ],
    "correctAnswer": "A",
    "audioUrl": "WEN_01_read_full.mp3",
    "imageUrl": "WEN_01_illus_room.jpg"
  }
]

### 要求：
- 输出纯 JSON，不要包含任何额外解释或 Markdown 标记（如 ```json）。
- 确保 JSON 格式正确，可以被 `JSON.parse()` 解析。
- 如果某行数据不完整（例如缺少题干或选项），请跳过该行并在输出中忽略（或给我一个警告，但最终只输出有效数据）。

请开始转换。