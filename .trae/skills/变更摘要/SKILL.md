---
name: 变更摘要
description: 并在每次修改代码后自动完成以下操作
---

请在项目根目录创建一个 CHANGELOG.md 文件（如果已存在则追加），并在每次修改代码后自动完成以下操作：

生成一条简洁的变更摘要，包含日期、修改目的、修改文件列表和功能影响。

将该条摘要追加到 CHANGELOG.md 顶部（最新在上）。

使用 GitHub MCP 将这次变更（包括代码和 CHANGELOG 文件）自动提交到一个新分支，分支名用 feature/日期-功能描述 的格式，并创建 Pull Request。