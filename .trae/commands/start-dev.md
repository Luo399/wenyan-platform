---
name: 'start-dev'
description: '启动前后端开发环境'
---

执行启动开发环境所需的所有步骤，包括后端和前端。

1. 打开一个新的终端窗口，进入 `backend` 目录。
2. 在该目录下运行 `node server.js`，等待输出 "后端运行在 <http://localhost:3000"。>
3. 再打开另一个新的终端窗口，回到项目根目录。
4. 在根目录下运行 `npm run dev`，等待输出 "Local: <http://localhost:5173/"。>
