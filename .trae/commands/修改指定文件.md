---
name: "修改指定文件"
description: "改动范围限制"
---

<br />

- 只允许修改 我指定的 文件或函数。
- 如果需要修改其他文件（如 store、工具函数、路由、父组件），必须先列出文件名并征求我的同意，不能直接改动。
- 所有新增的修改器、解码器必须写在 WordList.vue 文件内部，不要抽取为公共方法。
- 保持项目中其他所有文件的现有功能完全不变，只解决当前组件的问题。

